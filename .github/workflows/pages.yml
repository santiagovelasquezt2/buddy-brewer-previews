name: Deploy Pages
# GitHub Pages is served from the gh-pages branch (legacy/branch source).
# Do not run actions/deploy-pages here — that would switch the site to Actions and break previews.
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Pages are published from the gh-pages branch."
